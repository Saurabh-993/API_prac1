Just creation of Practice API

WE WILL Create API with expressJS FrameWork

we will create hybrid responses with this API 
    - we will create static and dynamic routing.
    - /users (This will be for the browser where the HTML can be readed).
    - /api/users (This will provide the responses in JSON format).

we will use all the methods i.e
    - Get (for retreiving data from the server by both methods API (JSON responses) as well as normal browser HTML responses).
    - Post , Patch & Put all these methods will be done after this through Postman.